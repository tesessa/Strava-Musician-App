# Demo mode: use fake data instead of the real API.
# Set to "true" for demo/development without a backend.
VITE_DEMO_MODE=false
VITE_SERVER_URL=http://localhost:3001

# Optional: override Oracle pre-authenticated request base URLs (each must end with /o/)
# VITE_ORACLE_PAR_AUDIO_BASE_URL=
# VITE_ORACLE_PAR_VIDEO_BASE_URL=
# VITE_ORACLE_PAR_PROFILE_IMAGES_BASE_URL=